import { FC } from 'react';
import IconProps from '../../../props/Icon';
/**
 * Add
 *
 * @param {object} props
 * @param {string} props.color
 * @param {number} props.size
 * @returns {React.ReactNode}
 */
declare const IconAdd: FC<IconProps>;
export default IconAdd;
